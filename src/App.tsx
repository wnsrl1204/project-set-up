import React from "react";
import "../css/global.css";
import styles from "../css/app.module.css";

type Props = {};

const App = (props: Props) => {
  return <div className={styles.title}>App</div>;
};

export default App;
